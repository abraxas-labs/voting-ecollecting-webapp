/**
 * (c) Copyright by Abraxas Informatik AG
 *
 * For license information see LICENSE file.
 */

// use another name than file to avoid conflicts (File is a built-in type)
export interface StoredFile {
  id: string;
  name: string;
}
